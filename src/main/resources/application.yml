spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vendasdb
    username: cytrus
    password: cytrus123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: cytrus
    password: cytrus123
    exchange: venda.exchange
    rounting-key: venda.routing.key
server:
  port: 8080
